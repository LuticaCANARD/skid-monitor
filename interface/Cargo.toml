[package]
name = "interface"
version.workspace = true
edition.workspace = true

# 라이브러리 크레이트: client와 server가 공유하는 프로토콜과 데이터 타입을 제공한다.
[lib]
name = "interface"
path = "src/lib.rs"

[dependencies]
# 신호 타입을 TCP 경계 너머로 보내기 위한 직렬화. OTel 의존성은 두지 않는다(양쪽이 공유하는 가벼운 계약).
serde = { workspace = true }
